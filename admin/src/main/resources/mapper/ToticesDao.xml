<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.ant.admin.dao.ToticesDao" >

    <!-- 可根据自己的需求，是否要使用 -->
    <resultMap id="toticesMap" type="com.ant.admin.entity.Totices">
        <result column="toticesId" property="totices_id"/>
        <result column="userId" property="user_id"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="publishDate" property="publish_date"/>
        <result column="updateDate" property="update_date"/>
        <result column="publishUser" property="publish_user"/>
        <result column="updateUser" property="update_user"/>
    </resultMap>
    <select id="find" parameterType="Map" resultType="toticesMap">
        select * from t_totices
        <where>
            1=1
        </where>
    </select>
    <select id="findAll" resultType="toticesMap">
        select * from t_totices
    </select>
</mapper>